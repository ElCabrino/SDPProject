<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ch.epfl.sweng.vanjel">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity"></activity>
        <activity android:name=".ChooseRegistration" />
        <activity android:name=".Registration" />
        <activity android:name=".DoctorCalendar" />
        <activity android:name=".Profile" />
        <activity android:name=".PatientInfo" />
        <activity android:name=".FilteredDoctors" />
        <activity android:name=".SearchDoctor" />
        <activity android:name=".DoctorAvailability">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>